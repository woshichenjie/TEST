<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liu.mapper.GirlInfoMapper">

	<!-- 添加 -->
	<insert id="addGirl" parameterType="GirlInfo">
		insert into girl (age,cup_size)
		values(#{age},#{cup_size})
	</insert>
   
   
   <!-- <select id="select" parameterType="select">
   		select * from girl
   </select> -->
</mapper>